&НаКлиенте
Процедура ПодключитьВК()
    Перем ОписаниеОшибки;
    Если ОбъектВК = Неопределено Тогда	
        ОбъектВК = Новый("AddIn.MyVK.RDPClientNativeAPI1C");
    КонецЕсли;
    
    Попытка
        // Здесь может быть ваш код подключения
    Исключение
        Сообщить("Ошибка подключения к DLL: " + ОписаниеОшибки);
    КонецПопытки;
КонецПроцедуры
